name: Output information about new issue created in Github
on:
  issues:
    types: [opened, reopened]
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Output GitHub context
        run: |
          echo "${{ toJson(github) }}"