name: Artifact demo
on:
  push:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v3
      - name: Install dependencies
        run: |
          cd artifact-app
          npm ci
      - name: Lint code
        run: |
          cd artifact-app
          npm run lint
      - name: Test code
        run: |
          cd artifact-app
          npm run test
  build:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v3
      - name: Install dependencies
        run: |
          cd artifact-app
          npm ci
      - name: Build code
        run: |
          cd artifact-app
          npm run build
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: artifact-app
          path: |
            artifact-app/dist/
            artifact-app/package.json
  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v3
      - name: Deploy application
        run: |
          echo "Deploying artifact-app"